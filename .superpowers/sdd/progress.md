# Progress Ledger — Offline Support (Scanner Precache / PDF Cache / Scan Queue / Network Hint)

Plan: docs/superpowers/plans/2026-09-11-offline-support-phase1-3.md
Worktree: /home/user/Karteikarten-Manager/.claude/worktrees/offline-support-phase1-3 (branch worktree-offline-support-phase1-3, based on beta@0736021)

## Tasks

Task 5: complete (Verifikation, keine Code-Änderungen). Scope vom Controller beim Dispatch bewusst angepasst: die im Brief erwähnten alten race_test.js/retry_test.js/backup_fallback_test_idb.js aus der separaten, bereits abgeschlossenen und gemergten IndexedDB-Migration dieser Session existierten nicht mehr (lagen in einem inzwischen entfernten Worktree/scratch-Bereich) - stattdessen alle 6 in DIESEM Worktree während Task 1-4 entstandenen Playwright-Tests erneut gegen eine frisch generierte Test-HTML laufen lassen. node --check auf index.html UND sw.js: beide sauber. IndexedDB-Key-Kollisionsfreiheit bestätigt (ein Object-Store, exakte String-Gleichheit beim Zugriff, neue Keys pdfCacheIndex/scanUploadQueue/pdfblob:<id> strukturell nie mit data/dataBackup verwechselbar). Alle 6 Regressionssuiten (sw_heavy_precache, pdf_cache + fix_verify, scan_queue + fix_verify, network_offline_hint) erneut ausgeführt: 76/76 Assertions PASS, keine Regression durch den letzten Controller-Fix (onSavePhoto-Guard). online-Event-Listener-Koexistenz manuell im Quelltext bestätigt: 3 unabhängige Registrierungen (LernSpaceView isOnline-State, bestehender Boot-Retry aus der IndexedDB-Migration, neuer Scan-Queue-Flush), jede mit eigenem Handler/Cleanup, keine gemeinsam mutierten Variablen, keine gegenseitige Störung.)

Task 4: complete (commit aba247f + Nachbesserung dbb39cf + Controller-Politur direkt danach, gepusht). Offline-Guard (if(!isOnline){showToast(...);return;}) an insgesamt 17 Netzwerk-Schreibfunktionen in LernSpaceView ergänzt (11 in Runde 1, 5 in der Nachbesserung, 1 vom Controller nach Review-Fund direkt gefixt: onSavePhoto/ls_group_set_photo). Implementer fand + fixte selbst einen echten Bestandscode-Bug: LernSpaceView hatte gar keinen h(Toast,{msg:toast})-Render, der neue Offline-Toast wäre unsichtbar geblieben - Controller verifiziert (Diff bestätigt 0 Treffer davor). Bereits vorhandener Offline-Banner wiederverwendet statt Duplikat. Review-Runde 1: Implementer meldete selbst 6 übersehene RPC-Schreibfunktionen (Korrekturliste im Dispatch war unvollständig) - 5 davon hatten echte getSb().rpc()-Aufrufe und wurden nachgerüstet, declineGroupInvite bewusst ausgelassen (macht nur lsGet/lsSet, kein .rpc(), hat bereits sichtbares Fehler-Feedback) - vom Controller direkt am Code verifiziert. applyGroupUpdates Guard gibt bewusst Promise.resolve() statt bare return zurück, da alle 4 Aufrufer .catch() auf den Rückgabewert hängen - vom Controller verifiziert, keine Abweichung im negativen Sinne. Review (beide Runden zusammen): spec compliance NICHT vollständig bei erstem Durchlauf - Reviewer fand per eigener vollständiger grep-Suche über alle getSb().rpc( in LernSpaceView noch EINE weitere ungeguardete Stelle (onSavePhoto, GroupAvatarPickerModal-Callback, ruft ls_group_set_photo, zusätzlich verschärft durch optimistisches UI-Update vor dem RPC-Aufruf) - vom Controller direkt am Code bestätigt und selbst gefixt (kein weiterer Fix-Subagent nötig für eine Zeile), node --check danach sauber. Reviewer bestätigte beide Controller-Vorab-Prüfungen (Promise.resolve, declineGroupInvite-Auslassung) eigenständig als korrekt. Banner- und Toast-Fixes vom Reviewer unabhängig verifiziert. 2 MINOR-Funde protokolliert für die finale Gesamt-Review, nicht blockierend: (a) Guard bei createGroup/inviteMember steht vor bestehenden lokalen Validierungen, wodurch offline+ungültige Eingabe jetzt "Offline" statt der spezifischeren Meldung zeigt - funktional unschädlich, nur Meldungspriorität verschoben; (b) saveAvatar und acceptShared/declineShared (saveDataRemoveShared-Konsumenten) bleiben ungeguarded wie declineGroupInvite - konsistent mit der .rpc()-fokussierten Abgrenzung dieser Aufgabe, aber strenggenommen ebenfalls Netzwerk-Schreibpfade mit optimistischem State - außerhalb des expliziten Auftragsumfangs, evtl. Folgeaufgabe.)

Task 3: complete (commit 8608f54, Fix-Commit 4f75d29, gepusht). queueScanForUpload/blobToDataURL/dataURLToBlob/flushScanQueue hinzugefügt, confirmScanName so umgebaut dass ein fehlgeschlagener Drive-Upload den Scan in die Warteschlange schickt statt Fehler zu zeigen, online-Listener + Boot-Versuch für automatisches Abarbeiten ergänzt. Brief-Zeilennummern waren veraltet (Datei hatte sich seit Planerstellung verschoben) - Implementer hat per Textabgleich die korrekten Stellen gefunden, keine Regelverletzung. Review: spec compliance PASS (zeichengenau), code quality: 1 CRITICAL + 1 IMPORTANT-Fund, beide vom Controller nachvollzogen und bestätigt - (1) CRITICAL: flushScanQueue hatte keinen Re-Entrancy-Schutz, ein Boot-Aufruf + ein kurz danach feuerndes online-Event konnten parallel laufen und denselben Scan doppelt zu Drive hochladen; (2) IMPORTANT: ungeschütztes Read-Modify-Write auf denselben IDB-Key zwischen queueScanForUpload und flushScanQueue konnte einen waehrend eines laufenden Flushs neu hinzugefügten Scan durch einen veralteten remaining-Snapshot stillschweigend überschreiben/verlieren. Beide per Fix-Subagent behoben: Modul-Flag _scanFlushInFlight verhindert parallele Durchläufe; flushScanQueue liest vor jedem einzelnen Entfernen die Warteschlange frisch statt aus einem einmaligen Anfangs-Snapshot. Fix-Verifikation: bestehende 16 Assertions weiterhin PASS, 2 neue gezielte Regressionstests (7 Assertions) PASS, zusätzlich gegen den Vorher-Code laufen lassen um zu beweisen, dass sie die Bugs wirklich erkennen (4/7 schlugen dort wie erwartet fehl). Vom Reviewer als bereits korrekt bestätigt (keine Fixes nötig): dataURLToBlob ist binär-sicher (atob+charCodeAt, kein PDF-Korruptionsrisiko), Mid-Flush-Verbindungsabbruch verliert keine Einträge (remaining/filter-Logik war schon vorher korrekt), Koexistenz mit dem bestehenden onBackOnline-Listener ist sauber (separater useEffect). 1 MINOR-Fund protokolliert, nicht blockierend: Roundtrip-Test nutzte nur Text-Payload statt echtem Binärinhalt mit High-/Null-Bytes - Implementierung korrekt, Testabdeckung könnte robuster sein. Test-Abdeckung: confirmScanName selbst (closure-scoped in App(), ohne volle Kamera/Crop-UI nicht erreichbar) wurde per Nachbildung + statischem Regex-Source-Check gegen den echten Quelltext getestet, nicht direkt - vom Reviewer als vertretbare, aber nicht lückenlose Einschränkung bewertet, konsistent mit dem Task-2-Präzedenzfall.)

Task 2: complete (commit 106e0cb, Fix-Commit 4821554, gepusht). pdfCacheGet/pdfCachePut + kkmIdbDelete hinzugefügt, prefetchDocFile umverdrahtet (Cache-Check zuerst, Netzwerk-Fallback, Hintergrund-Cache-Schreiben). Implementer-Entscheidung, vom Reviewer bestätigt: kkmIdbDelete statt kkmIdbSet(key,null) - echtes Löschen statt "Key bleibt mit Wert null bestehen" (selbst verifiziert). Review: spec compliance PASS, code quality: 2 IMPORTANT-Funde vom Controller nachvollzogen und bestätigt - (1) Selbst-Doppelzählung wenn eine Datei erneut gecacht wird während ihr alter Index-Eintrag noch da ist (führte zu unnötiger Verdrängung anderer Einträge), (2) Index/Blob geraten auseinander wenn ein Verdrängungs-Löschversuch fehlschlägt (Index behauptet "weg", Blob bleibt verwaist liegen, für immer außerhalb der 150MB-Buchführung). Beide per Fix-Subagent behoben: Fix 1 filtert fileId aus der eigenen Verdrängungsrechnung heraus; Fix 2 entfernt den Index-Eintrag erst NACH erfolgreichem kkmIdbDelete statt synchron/unabhängig davon. Fix-Verifikation: bestehende 18 Assertions weiterhin PASS, 6 neue gezielte Regressionstests PASS, zusätzlich gegen den Vorher-Code laufen lassen um zu beweisen, dass sie die Bugs wirklich erkennen (3 davon schlugen dort wie erwartet fehl). 3 MINOR-Funde protokolliert für die finale Gesamt-Review, nicht blockierend: (a) pdfCacheGet's returned promise wartet auf den lastAccess-Index-Schreibvorgang statt sofort mit dem Buffer aufzulösen (matcht aber den Brief wörtlich, keine Abweichung); (b) Index-Schreiben vor Blob-Schreiben in pdfCachePut kann bei fehlschlagendem Blob-Schreiben einen "Phantom"-Indexeintrag ohne Blob erzeugen (bewusster Kompromiss laut Reviewer - besser als ein unsichtbarer verwaister Blob); (c) ein Blob ohne Index-Gegenstück (erreichbar über (b)) wird nie per lastAccess aktualisiert und kann nie verdrängt werden. Test-Abdeckung: prefetchDocFile selbst (closure-scoped in App(), ohne vollen Auth-Boot nicht erreichbar) wurde nicht direkt getestet, stattdessen eine Nachbildung der exakten Schritt-2-Komposition, per Regex gegen den echten Quelltext abgeglichen - vom Reviewer als legitime, begründete Einschränkung bewertet.)

Task 1: complete (commit 2aabcf7 + Controller-Politur-Commit direkt danach). HEAVY-Array (7 Scanner-Dateien) + separater, nicht-blockierender activate-Listener für den Hintergrund-Precache hinzugefügt, sw.js:1-97. Implementer-Verifikation: Node-Skript extrahiert HEAVY aus dem Quelltext und prüft jede Datei per fs.existsSync gegen das Repo-Root (echte SW-Registrierung unter file:// als unzuverlässig verworfen, nachvollziehbar begründet) - alle 7 Dateien vorhanden, node --check sauber. Review: spec compliance PASS (byte-genauer Treffer zum Brief). Code quality: 1 IMPORTANT-Fund (Promise.all ohne .catch()) - Controller hat das selbst geprüft und als technischen Fehlalarm eingestuft (cacheWithRetry() aus dem bestehenden, unveränderten Code löst im eigenen .catch() bei retries<=0 einfach mit `return;` auf, kann also nie rejecten - Promise.all darüber kann folglich auch nie rejecten). Trotzdem als billige, sinnvolle Absicherung direkt vom Controller nachgezogen (kein Fix-Subagent nötig für eine Zeile): .catch(function(){}) ergänzt, plus die vom Reviewer als Minor genannte ungenutzte e-Parameter entfernt. node --check erneut sauber nach der Politur.

