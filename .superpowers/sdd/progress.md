# Progress Ledger — Bug-Fix Sweep (systematische Bug-Suche, 28 Funde)

Worktree: /home/user/Karteikarten-Manager/.claude/worktrees/bug-fix-sweep (branch worktree-bug-fix-sweep, based on beta@ebb94c7)
Quelle: systematischer Bug-Audit über den kompletten App-Code (6 parallele Agents + Controller-Direktprüfung von sw.js/api/manifest.json/updates.json/vercel.json), von Sophie freigegeben: alle 29 Funde außer #20 (api/report.js bleibt bewusst offen, Issues sollen ankommen).

## Gruppen (nach Bereich)

Gruppe D (Kalender): complete (commit 03fcff6, gepusht, vom Controller direkt gegengelesen statt separater Reviewer-Dispatch - keine Critical/Important-Risikoklasse, die das erfordert hätte). 3 Funde behoben: (1) Important — Termin-Formular bot immer 1-31 Tage unabhängig vom Monat an ("31. Februar" möglich) - neue daysInMonth(monat,jahr)-Funktion berechnet TAG_OPTIONS dynamisch (inkl. korrekter Schaltjahr-Behandlung), setDatePart() klemmt einen bereits gewählten Tag beim Monats-/Jahreswechsel zusätzlich auf den letzten gültigen Tag statt ihn ungültig zu lassen; (2) Important — "Heute"-Sektion zeigte nur echte überfällige Termine, ein Termin mit heutigem Datum landete faelschlich unter "Bevorstehend" - Grenzen von </>= auf <=/> verschoben; (3) Minor — Sortier-Komparator verletzte die Symmetrie bei gleichem Datum (lieferte nie 0), jetzt korrekt. Verifikation: node --check sauber, 28/28 Assertions PASS inkl. echtem daysInMonth-Logikreplay für mehrere Schaltjahre (2026/2028/2000/1900) und Live-UI-Interaktion (Formular, Tag-Klemmung, "Heute"-Zuordnung, Sortier-Stabilität über mehrere Renders). Einziger Hinweis: JAHR_OPTIONS (aktuelles Jahr + 1) enthält kein Schaltjahr, Schaltjahr-Fall deshalb per direktem Funktionsaufruf statt über die echte UI-Jahresauswahl getestet - nachvollziehbar, kein Mangel.

Gruppe C (Sync/Auth): complete (commit 2808a93 + Nachbesserung 0c374c6 + Controller-Politur direkt danach, gepusht). Der wichtigste Fix im gesamten Sweep: (1) Critical, Datenverlust — patch() aktualisiert den React-data-State direkt, aber _storeCache (die synchrone "Quelle der Wahrheit" für loadStore() aus der IndexedDB-Migration) wurde bisher nur über einen verzögerten useEffect(fn,[data]) (läuft nach dem React-Commit) synchronisiert; drei Sync-Vergleiche (Realtime-Handler, Login-Restore-Check, Push-Vergleich) nutzen loadStore()._syncedAt - traf in diesem kurzen Zeitfenster ein Remote-Update ein (älter als die frische lokale Änderung, aber neuer als der noch-veraltete _storeCache-Wert), überschrieb der ältere Remote-Stand fälschlich frischen, noch nicht persistierten lokalen Fortschritt. (2) Minor — bindGuestAuthUid() hatte keinen Parallel-Schutz wie handleAuthUser()'s etabliertes _authUserInFlight-Muster, konnte zu doppelten ls_claim_own_id-RPCs führen. ERSTER LÖSUNGSVERSUCH (Commit 2808a93) implementierte für Fix 1 einen Existenz-Guard (_storeCache!==null) direkt im App()-Render-Körper - die anschließende Gesamt-Review (auf dem stärksten Modell, besonders gründlich wegen der Kritikalität) fand dabei einen ernsten Gegenfund, vom Controller selbst am Code nachvollzogen und bestätigt: dieser Guard prüft nur ob _storeCache befüllt ist, nicht ob data (der React-State) das schon widerspiegelt - loadStoreInitial() setzt _storeCache synchron auf echte Daten, BEVOR der zugehörige setData(s)-Commit tatsächlich gerendert wird (React 18/createRoot verzögert das). Löst in diesem Fenster IRGENDEIN anderer State-Wechsel in App() einen Zwischen-Render aus, hätte die alte Guard-Zeile die frisch geladenen ECHTEN Daten in _storeCache mit dem noch-leeren data-Platzhalter überschrieben - der exakt gegenteilige Datenverlust-Bug. NACHBESSERUNG (Commit 0c374c6): Guard von Existenz-Prüfung auf Inhalts-Vergleich (_syncedAt-Zeitstempel) umgestellt - `if(_storeCache && data && (data._syncedAt||0)>=(_storeCache._syncedAt||0) && _storeCache!==data){ _storeCache=data; }`, behebt weiterhin den ursprünglichen Bug (echter patch() liefert immer ein neueres _syncedAt), vermeidet aber den Gegenfund (im gefährlichen Zwischen-Render-Fenster hat der Platzhalter _syncedAt=0, immer kleiner als die echten, bereits geladenen Daten - Guard feuert nicht, _storeCache bleibt korrekt). Gleichzeitig Fix 2 um 2 zusätzliche, von der Review gefundene Lücken ergänzt: withTimeout(...,12000) um den RPC-Aufruf (fehlte, hätte bei einer haengenden Verbindung das In-Flight-Flag dauerhaft blockieren können, analog zu einem bereits dokumentierten Fix bei handleAuthUser); _pendingDeliberateGuestBind-Flag, damit eine bewusste "Als Gast fortfahren"-Absicht (deliberate=true) nicht verloren geht, wenn sie während eines schon laufenden Hintergrund-Binds ankommt - wird beim Abschluss des laufenden Binds nachträglich eingelöst statt verworfen. Diese Nachbesserung wurde durch einen Session-Rate-Limit-Abbruch unterbrochen (Code bereits korrekt fertig im Working Tree, nur Verifikation+Commit fehlten) - ein Folge-Agent hat den bereits korrekten, unveränderten Code übernommen (nicht neu geschrieben) und nur noch die Verifikation nachgeholt. Fand dabei selbst einen Konstruktionsfehler in seinem EIGENEN ersten Testaufbau (nicht im Produktcode) - ein Test für Lücke B schlug fehl, weil auf einem brandneuen Testgerät dataReady direkt am Abschluss des Hintergrund-Binds hängt und die Race dadurch gar nicht beobachtbar war; nach Umbau auf ein realistisches "wiederkehrender Gast mit echten Lokaldaten"-Szenario wurde die Race korrekt reproduzierbar. Verifikation: node --check sauber, 41/41 Assertions PASS (18 aus Runde 1 weiterhin bestehend + neue), inkl. des entscheidenden reversed-bug-Tests (echte App()-Komponente, _storeCache mit echten Seed-Daten befüllt, unabhängiger Render per flushSync ausgelöst bevor der Boot-setData(s)-Commit lief, _storeCache bleibt korrekt bei den echten Daten) und eines mit Negativkontrolle abgesicherten Lücke-B-Tests. Controller hat zusätzlich eine vom Folge-Agent selbst ehrlich gemeldete, kleine Restlücke direkt gefixt (kein weiterer Subagent-Durchlauf nötig): der withTimeout-REJECT-Pfad (echter 12s-Timeout) setzte _pendingDeliberateGuestBind nicht zurück, wodurch eine übersprungene bewusste Absicht nach einem tatsächlichen Timeout auf unbestimmte Zeit "hängen" bliebe statt zeitnah aufgeräumt zu werden - jetzt wird das Flag auch im Fehlerfall zurückgesetzt. node --check nach der Politur erneut sauber.

Gruppe B (Drive/Scan/Dokumente): complete (commit 01a75f9, gepusht). 3 Funde behoben: (1) Critical — driveFetch()/driveUploadPdf() und weitere Schreibfunktionen prüften nie den HTTP-Status, fehlgeschlagene Drive-Uploads/-Änderungen wurden als Erfolg gemeldet (stiller Datenverlust bei Scans/Direkt-Uploads) — neue Hilfsfunktion driveFetchJson() prüft r.ok und rejected mit der echten Google-Fehlermeldung, umgestellt: driveUploadPdf, driveCreateFolder, driveMoveFile, driveRenameFile; driveDeleteFile separat gefixt (eigener r.ok-Check statt driveFetchJson, da Google DELETE 204 ohne Body liefert und r.json() dort werfen würde — vom Implementer korrekt erkannt und sauber gelöst); (2) Important — handlePdfUpload() (Direkt-Upload, nicht Scan) legt fehlgeschlagene Uploads jetzt ebenfalls über die bestehende queueScanForUpload() in die Offline-Warteschlange, analog zum Scan-Weg; (3) Important — neues Generation-Token scanCropGenerationRef verwirft veraltete Kantenerkennungs-/Zuschnitt-Ergebnisse nach Abbruch/Neustart einer Foto-Session, cancelScanCrop() erhöht die Generation zusätzlich eigenständig. Implementer fand + fixte zusätzlich driveDeleteFile (gleicher Root-Cause wie Fix 1, außerhalb des wörtlichen Briefs, aber folgerichtig). Review: spec compliance PASS (alle Aufrufstellen einzeln gegen den echten Code verifiziert, nicht nur gegen den Bericht), code quality PASS, keine Findings. Reviewer bestätigte eigenständig: driveListChildren liefert bei einem Fehler weiterhin [] statt einen Fehler zu zeigen (Ordner wirkt fälschlich leer) — bewusst als reiner Lesevorgang außerhalb des Scopes belassen, für Sophie zur Kenntnis notiert, kein neuer Fix nötig für diese Gruppe.

Gruppe A (Core-Infra/Storage/Service-Worker): complete (commit c6b9048 + Fix-Commit 685523a, gepusht). 4 von 5 Funden behoben: (1) Critical — sw.js CRITICAL-Liste erweitert um Supabase (CDN.slice(0,2)→slice(0,3)), verhindert dass alte vollständige Caches gelöscht werden obwohl Supabase nie gecacht wurde (getSb() wirft sonst eine echte Exception bei komplett-offline-Start); (2) Important — saveStore() zeigt bei fehlgeschlagenem primären IndexedDB-Write jetzt einen gedrosselten (max. 1x/30s) Toast über das bestehende _syncShowToast-Muster; (3) Important — sw.js Tages-Erinnerung bekommt ein zusätzliches Sicherheitsnetz (maybeNotify() zusätzlich bei activate() und gedrosselt bei jedem fetch-Event, bestehende Timer unverändert); (4) Minor — kkmIdbOpen() memoisiert jetzt die IndexedDB-Verbindung statt bei jedem Aufruf neu zu öffnen. (5) Minor bewusst ausgelassen (syncAppData-Atomarität) — nachvollziehbar als bereits nah am Optimum ohne echte Cross-System-Transaktion begründet. Review fand 2 echte IMPORTANT-Folgefehler in den Fixes selbst, beide vom Controller bestätigt und per Fix-Subagent behoben: (a) kkmIdbOpen()s Memoisierung hatte selbst eine Race Condition — prüfte nur die fertige Instanz, nicht die laufende Öffnung, wodurch zwei synchron kurz nacheinander aufgerufene saveStore()-Schreibvorgänge (data+dataBackup beim allerersten Aufruf einer Session) trotzdem zwei parallele Verbindungen öffneten und eine davon für immer offen/ungetrackt blieb — behoben durch zusätzliche In-Flight-Promise-Memoisierung; (b) beide neuen maybeNotify()-Aufrufe in sw.js liefen ohne e.waitUntil(), wodurch der Service Worker mittendrin terminiert werden konnte, bevor die Prüfung abgeschlossen war — behoben durch e.waitUntil()-Einbindung an beiden Stellen, ohne den bewusst nicht-blockierenden HEAVY-Precache im selben activate-Listener zu beeinträchtigen. Verifikation: node --check auf beiden Dateien sauber, 10/10 + 8/8 Assertions PASS über beide Runden. Controller hat den finalen Diff eigenständig gegengelesen, exakter Treffer zu beiden Fix-Briefs.

